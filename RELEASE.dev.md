## Notice

> **This is a rolling `develop`-branch build**, overwritten automatically on
> every push — not a tagged, curated release. It has not been through the
> manual review a `stable`/`testing` release gets, and can change or regress
> at any time. Use it to preview upcoming changes, not on a printer you
> depend on.

## New Features and Key Changes

<!-- CHANGELOG_PLACEHOLDER -->

## Checksums

<!-- CHECKSUMS_PLACEHOLDER -->
